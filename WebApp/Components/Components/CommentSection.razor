@rendermode InteractiveServer
<div class="col container-fluid">
    <div class="row row-cols-1">
        @for (int i = 0; i < 12; i++)
        {
            if (i == 0)
            {
                <UserComment IsNewComment="@true" />
                continue;
            } 
            <UserComment />
        }
    </div>
</div>
@code {
    
}